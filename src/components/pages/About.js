import React, { Fragment } from "react";

const About = () => {
    return (
        <Fragment>
            <h1>About the App</h1>
            <p>Search Github users</p>
        </Fragment>
    );
};

export default About;
